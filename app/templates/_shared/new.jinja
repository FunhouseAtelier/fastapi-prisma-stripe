{# app/templates/_shared/new.jinja #}

{% extends "__layout.jinja" %}

{% block title %}New {{ mod_title }}{% endblock title %}

{% block content %}
  <main class="xl new">
    <a href="{{ url_for('get_home') }}" class="close-icon" aria-label="Close">
      🗙
    </a>
    <h1>New {{ mod_title }}</h1>
    <form method="post">
    {% block form_fields %}{% endblock form_fields %}
    <div class="button-group">
      <button href="{{ url_for(create_handler) }}">Create {{ mod_title }}</button>
      <a href="{{ url_for('get_home') }}" class="cancel">Cancel</a>
    </div>
    </form>
  </main>
{% endblock content %}
